{
  "name": "prototype",
  "version": "1.0.0",
  "description": "Root runner for client + server",
  "scripts": {
    "start": "concurrently --kill-others-on-fail \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
